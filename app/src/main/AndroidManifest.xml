<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.boostedpenguin.clipboardmanager">

    <application
        android:name=".NoteApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClipboardManager">
        <activity
            android:name=".CreateNoteActivity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ClipboardManager.NoActionBar"/>
        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ClipboardManager.PreferenceScreen" />
        <activity
            android:name=".MainActivity"
            android:label="ClipboardManager"
            android:theme="@style/Theme.ClipboardManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>